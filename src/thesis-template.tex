% config file
\input{\pathtotemplate/src/config-template.tex}

% custom config file
\input{custom-config.tex}

% titlepage
\input{\pathtotemplate/src/titlepage.tex}

% custom titlepage
\input{custom-titlepage.tex}

% Chaptercation
\usepackage{titlesec}

\titleformat{\chapter}[hang]
    {\chapterstyle} % style of chaptercation
    {\thechapter} % only chapternumber
    {\chapterspace} % spacing between chapternumber and title 
    {\chaptertitlestyle} % style of chaptertitle

% Head and foot lines
\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{} % clear all header and foot
\fancyhead[LE,RO]{\leftmark} % left header: chaptername
\fancyfoot[LE]{\hspace{\pagenumberposition}\thepage} % pagenumber for even pages 
\fancyfoot[RO]{\thepage\hspace{\pagenumberposition}} % pagenumber for uneven pages

% Chapterpages use style plain, configure in the same way 
\fancypagestyle{plain}{
    \fancyhf{} 
    \fancyhead[LE,RO]{\leftmark} 
    \fancyfoot[LE]{\hspace{\pagenumberposition}\thepage} 
    \fancyfoot[RO]{\thepage\hspace{\pagenumberposition}}
}


% Page geometry
\usepackage{geometry}

\geometry{
    \format,
    inner=\innerspacing, % inner distance to edge 
    outer=\outerspacing, % outer distance to edge 
    top=\topspacing, % top distance to edge 
    bottom=\bottomspacing, % bottom distance to edge
    heightrounded, % improves layout 
}

% Language
\usepackage[\languageoption]{babel}

% References
\usepackage[style=\citationstyle]{biblatex} % defines citationstyle 
\usepackage{csquotes} % necessary package

% Blindtext fürs testen
\usepackage{blindtext}

% Sinvolle Kommandos
\newcommand{\startpagenumberinghere}{\setcounter{page}{1}}
\newcommand{\firstchapter}{
    \pagestyle{fancy}%
    \startpagenumberinghere%
}
\newcommand{\thesistitle}{
    \pagestyle{empty}
    \maketitle
}
\newcommand{\thesistoc}{
    \clearpage
    {
        \pagestyle{empty}
        \fancypagestyle{plain}
        {
            \fancyhf{}%
            \renewcommand{\headrulewidth}{0pt}%
            \renewcommand{\footrulewidth}{0pt}%
        }
        \tableofcontents
        \thispagestyle{empty}
    }
}


